name: Deploy to Hostinger

on:
  push:
    branches: [main]
    paths:
      - '**/*.html'
      - '**/*.css'
      - '**/*.js'
      - '**/*.json'
      - '**/*.xml'
      - '**/*.txt'
      - '**/*.php'
      - '.htaccess'
      - 'IMG/**'
      - 'icons/**'
      - 'api/**'
      - 'assets/**'
      - 'images/**'
      - '.github/workflows/hostinger-deploy.yml'
  workflow_dispatch:

jobs:
  deploy:
    name: Deploy via SFTP
    runs-on: ubuntu-latest
    timeout-minutes: 15
    concurrency:
      group: hostinger-deploy-${{ github.ref }}
      cancel-in-progress: true

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4
        with:
          fetch-depth: 1

      - name: Install lftp
        run: sudo apt-get update && sudo apt-get install -y lftp

      - name: Deploy to Hostinger
        env:
          HOST: ${{ secrets.HOSTINGER_FTP_SERVER }}
          USER: ${{ secrets.HOSTINGER_FTP_USERNAME }}
          PASS: ${{ secrets.HOSTINGER_FTP_PASSWORD }}
        run: |
          echo "🚀 Starting deployment..."
          lftp -u "$USER","$PASS" -p 65002 sftp://$HOST <<'EOF'
          set sftp:auto-confirm yes
          set net:max-retries 3
          set net:timeout 30
          set net:reconnect-interval-base 5
          set xfer:clobber on

          cd /home/u476428393/public_html/

          mirror -Rnev --delete --no-empty-dirs --parallel=4 \
            -x '(^|/)\.(git|github|vscode|claude)(/|$)' \
            -x '(^|/)(backups|logs|docs)(/|$)' \
            -X '.gitignore' \
            -X '.DS_Store' \
            -X '*.md' \
            -X '*.bak' \
            -X 'deploy*.sh' \
            -X '.deploy-trigger' \
            -X '.well-known/**' \
            . .
          cls -l
          cat .htaccess | sed -n '1,20p'

          echo "🧹 Cleaning up leaked directories..."
          rm -rf .git .github .vscode .claude

          bye
          EOF

          echo "✅ Deployment complete"

      - name: Verify live site updated
        run: |
          echo "⏳ Waiting 10 seconds for changes to propagate..."
          sleep 10
          echo "🔍 Fetching live homepage..."
          curl -sS https://chicagosmoney.com/ | tee /tmp/live.html
          echo "✅ Checking for index.html markers..."
          grep -q '<title>Chicago' /tmp/live.html || { echo "❌ Not updated"; head -c 500 /tmp/live.html; exit 1; }
