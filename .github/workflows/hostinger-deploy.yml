      - name: Deploy to Hostinger
        env:
          HOST: ${{ secrets.HOSTINGER_FTP_SERVER }}
          USER: ${{ secrets.HOSTINGER_FTP_USERNAME }}
          PASS: ${{ secrets.HOSTINGER_FTP_PASSWORD }}
        run: |
          echo "🚀 Starting deployment..."
          lftp -u "$USER","$PASS" -p 65002 sftp://$HOST <<'EOF'
          set sftp:auto-confirm yes
          set net:max-retries 3
          set net:timeout 30
          set net:reconnect-interval-base 5
          set xfer:clobber on
          cd /home/u476428393/public_html/
          mirror -Rnev --delete --no-empty-dirs --parallel=4 \
            --exclude-regex '(^|/)\.(git|github|vscode|claude)(/|$)' \
            --exclude-regex '(^|/)(backups|logs|docs)(/|$)' \
            --exclude-glob '.gitignore' \
            --exclude-glob '.DS_Store' \
            --exclude-glob '*.md' \
            --exclude-glob '*.bak' \
            --exclude-glob 'deploy*.sh' \
            --exclude-glob '.deploy-trigger' \
            --exclude-glob '.well-known/**' \
            . .
          cls -l
          cat .htaccess | sed -n '1,20p'

          echo "🧹 Cleaning up leaked directories..."
          rm -rf .git .github .vscode .claude

          bye
          EOF     # ← keep this indented exactly like the lines above

          echo "✅ Deployment complete"
